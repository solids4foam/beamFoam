/*--------------------------------*- C++ -*----------------------------------* \
| =========                 |                                                 |
| \\      /  F ield         | foam-extend: Open Source CFD                    |
|  \\    /   O peration     | Version:     4.0                                |
|   \\  /    A nd           | Web:         http://www.foam-extend.org         |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       volVectorField;
    object      DW;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

dimensions      [0 1 0 0 0 0 0];

internalField   uniform (0 0 0);

boundaryField
{
    // left
    // {
    //     type axialForceTransverseDisplacementNR
    //     value uniform (0 0 0);
    //     // refDisp uniform (0 0 0);
    //     axialForce uniform 100;
        
    //     refDispSeries
    //     {
    //         fileName "$FOAM_CASE/constant/timeVsRefDisplacement";
    //         outOfBounds clamp;
    //     }
        
    //     // type fixedValue;
    //     // value uniform (0 0 0);
    // }

    left
    {
        type forceBeamDisplacementNR;
        forceSeries
        {
            fileName        "$FOAM_CASE/constant/timeVsForce";
            outOfBounds     clamp;
        }
    }
    
    right
    {
        // type fixedDisplacement;
        
        type fixedPathDisplacement;
        value uniform (0 0 0);

        pathFileName "./constant/conicalPulleys/conicalPulleysPath";
        velocity 300;

        // displacementSeries
        // {
        //     fileName        "$FOAM_CASE/constant/timeVsDisplacement";
        //     outOfBounds     clamp;
        // }
    }
    
    // right
    // {
    //     type axialForceTransverseDisplacementNR
    //     value uniform (0 0 0);
    //     // refDisp uniform (0 0 0);
    //     axialForce uniform 0;

    //     refDispSeries
    //     {
    //         fileName "$FOAM_CASE/constant/timeVsRefDisplacement";
    //         outOfBounds clamp;
    //     }
    // }

    beam
    {
        type empty;
    }
}


// ************************************************************************* //
